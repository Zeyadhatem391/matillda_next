/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.unsplash.com",
      "cdn.pixabay.com",
      "i.ibb.co",
      "res.cloudinary.com",
    ],
  },
};

export default nextConfig;
